{
  "name": "Bootstrap GitOps on Cluster",
  "description": "Bootstrap GitOps on Cluster",
  "spec": [
    {
      "type": "multiplechoice",
      "question_name": "Cluster Name",
      "question_description": "Name of the cluster to deploy",
      "variable": "cluster_name",
      "required": false,
      "choices": ["proxmox", "vsphere"],
      "default": "vsphere"
    }
  ]
}
