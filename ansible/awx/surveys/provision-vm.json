{
  "name": "Provision a VM in Vcenter",
  "description": "Provision a VM in Vcenter",
  "spec": [
    {
      "type": "textarea",
      "question_name": "List of VMs to provision",
      "question_description": "JSON array of VM name and IPs",
      "variable": "vm_list",
      "required": true,
      "default": "[{ \"name\": \"\", \"ip\": \"\" }]"
    }
  ]
}
